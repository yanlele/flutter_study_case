name: flutterappdemo1
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  json_annotation: ^4.4.0
  shared_preferences: ^2.0.13
  provider: ^6.0.2
  dio: ^4.0.6
  cached_network_image: ^3.2.0
  fluttertoast: ^8.0.9
  flukit: ^2.0.0
  http_proxy: ^1.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.1.8
  json_model: ^1.0.0
  json_serializable: ^6.1.5

flutter:
  uses-material-design: true

flutter_intl:
  enabled: true
  class_name: GmLocalizations

